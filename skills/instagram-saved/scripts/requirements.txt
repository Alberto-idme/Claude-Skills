# Stage 1 browser source + browser hydration.
# After installing: playwright install chromium
playwright>=1.44

# Reel transcription. faster-whisper decodes audio via bundled PyAV, so it
# needs no system ffmpeg; openai-whisper is a drop-in alternative that does.
faster-whisper>=1.0

# On-screen text. ONNX-based, so no PyTorch, and its bundled models read
# Japanese, Korean and Chinese as well as Latin scripts.
rapidocr-onnxruntime>=1.3

# Video description and the extraction pass. Needs ANTHROPIC_API_KEY, or an
# `ant auth login` profile. Only these two stages use it.
anthropic>=1.0

# Frame decoding and JPEG encoding. Both usually arrive with the two above;
# pinned here so an OCR-only install still works.
av>=12.0
opencv-python-headless>=4.9
